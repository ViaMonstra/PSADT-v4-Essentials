# Exercise 2 – Explore the Remove-ADTFile Function

# Remove-ADTFile
Remove-ADTFile -Path "C:\Temp\deleteme.txt"

# Run installer silently
.\Invoke-AppDeployToolkit.exe -DeploymentType Install -DeployMode Silent
